doctype html
html(lang="en")
    head
        meta(charset="utf-8")
        meta(name="viewport" content='width=device-width initial-scale=1.0')
        title=title
        link(rel="stylesheet" href="./index.css")
    body
        h1 #{title}
        div
            h2 Propose
            ol
                li.
                    I can get the image URLs, alt text and page urls for a set of
                    images relating to a given search string.
                li.
                    I can paginate through the responses by adding a &offset=2 
                    parameter to the URL.
                li.
                    I can get a list of the most recently submitted search strings
        div
            h2 Example
            ul
                li
                    a(href=searchPath target='_blank') #{url}/#{searchPath}
                li
                    a(href=lotPath target='_blank') #{url}/#{lotPath}
                li
                    a(href=recentSearches target='_blank') #{url}/#{recentSearches}
        footer
            p &copy 2017 Developed by Luiko